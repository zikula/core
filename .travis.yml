language: php

php:
  - 5.3
  - 5.4

before_install:
  - pyrus install -o pear/PHP_CodeSniffer
  - pyrus install -o phpunit/PHP_CodeCoverage
  - pyrus install -o PHP_CodeCoverage
  - phpenv rehash

before_script: php vendors.php
