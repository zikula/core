{% extends '@ZikulaDefaultTheme/base.html.twig' %}

{% block themebody %}
    <div class="container 2col">
        <div class="row">
            <div id="themeLeftColumn" class="col-md-3">
                <p>left column</p>
            </div>
            <div id="themeMainContent" class="col-md-9">
                {{ admin|default ? adminHeader() : '' }}
                {{ showflashes() }}
                {{ maincontent|raw }}
                {{ admin|default ? adminFooter() : '' }}
            </div>
        </div>
    </div>
{% endblock themebody %}
