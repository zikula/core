{gettext}
<p>Block "Tree-like menu (menutree)" allows you to create the extended menu with nested structure. Below you will find some basic information.</p>

<h4>How to start?</h4>
<p>To <strong>add the first item</strong> to the menu, click link "<strong>Add</strong>". You will get a form in which you must enter the name of the item and possibly other parameters (title, address, css class). At the top of the form you can find drop-down list with installed languages, which allows to switch between languages.</p>
<p>While adding a new block, you can <strong>import</strong> an existing menu (supported blocks: "menu", "extmenu", "menutree" and "dynamenu"). To this purpose choose a block from the drop-down list.</p>

<h4>The basic options</h4>
<p>To <strong>move</strong> an item, simply <strong>grab a folder icon</strong> and drop it on desired position.</p>
<p><strong>Click the name</strong> of an item, to view a <strong>context menu</strong>, which includes the following options:</p>
<ul>
    <li><strong>Edit</strong>: opens the edit form</li>
    <li><strong>Delete</strong>: removes the selected element and all its subelements</li>
    <li><strong>Add new...</strong>
        <ul>
            <li><strong>before</strong>: opens a create form and adds the new item before the selected element</li>
            <li><strong>after</strong>: opens a create form and adds the new item after the selected element</li>
            <li><strong>as child</strong>: opens a create form and adds the new item as a child of the selected element</li>
        </ul>
    </li>
    <li><strong>Expand/Collapse this node</strong>: expands or collapses the selected node</li>
    <li><strong>Deactivate/Activate</strong>: turns on or off the selected element (and all its subitems) for the current language</li>
    <li><strong>State</strong>
        <ul>
            <li><strong>Activate for all languages</strong>: turns on the selected element (and all its subitems) for all the languages</li>
            <li><strong>Deactivate for all languages</strong>: turns off the selected element (and all its subitems) for all the languages</li>
        </ul>
    </li>
</ul>

<h4>Multilingual options</h4>
<p>If the site has many languages installed - the menu will include all language versions. Each menu element can a have separated name and title for each language. The URL address and css class may (but not need) be common for all languages.</p>
<p>The list of available languages is displayed above the menu (current language is marked). By clicking any language name you can change the displayed language.</p>

<h4>Dynamic elements</h4>
<p>Dynamic elements are menutree plugins that allows the use of automatically generated list in the menu. It may be a list of all modules, the list of categories, etc.</p>
<p>Dynamic elements are inserted in the same way as any other element of the menu. The only difference is that the url must use proper syntax, which looks like: (ext:Modulename:PluginName:AdditionalParams). The list generated by the plugin replaces the original menu item.</p>

<h5>Adminlinks</h5>
<p>This plugin generates a list of admin modules - similar to adminnav block.</p>
<pre>{literal}{ext:Blocks:adminlinks[:[flat,category]]}{/literal}</pre>
<ul>
    <li>Last parameter is optional. It can be flat and/or category separated by a comma.
        <ul>
            <li><strong>flat</strong> - will add the admin links in the current menu. Without flat the links are grouped one level down.</li>
            <li><strong>category</strong> - additionally groups the admin links by their category.</li>
        </ul>
    </li>
    <li>You can combine <strong>flat</strong> and <strong>category</strong> to have the category links added in the current menu.</li>
</ul>

<h5>Content pages</h5>
<p>Plugin for Content module. This plugin generates a list of Content pages.</p>
<pre>{literal}{ext:Blocks:Content:[groupby=page&amp;parent=1]}{/literal}</pre>
<ul>
    <li>Parameters in square brackets are optional:
        <ul>
            <li><strong>Groupby</strong> - allows to choose grouping method, allow values are menuitem (default) or page.</li>
            <li><strong>Parent</strong> - id of parent node - this allows to get specified node of content pages.</li>
        </ul>
    </li>
</ul>
<p>Note that this plugin considers if pages are enabled for display in menu. So if you wonder why certain items do not appear, look if you enabled this option at corresponding Content pages.</p>

<h5>Modules list</h5>
<p>This plugin generates a list of installed modules - similar to adequate option in standard menu.</p>
<pre>{literal}{ext:Blocks:modules:[flat]}{/literal}</pre>

<p>Parameter <strong>flat</strong> is optional and allows you to disable links grouping.</p>

<h5>News module</h5>
<p>Plugin for News module, generates a list of useful links to module and a list of categories.</p>
<pre>{literal}{ext:Blocks:news:[flat=BOOL&amp;links=view,add,cat,arch|ALL]}{/literal}</pre>

<ul>
    <li>Parameters in square brackets are optional:</li>
    <li><strong>Flat</strong> - true or false, default is false, the parameter allows you to disable links grouping</li>
    <li><strong>Links</strong> - a list of items to be displayed - default is all (ALL), available components are:
        <ul>
            <li><strong>View</strong> - link to main News view</li>
            <li><strong>Add</strong> - link to Submit News form</li>
            <li><strong>Cat</strong> - list of News categories</li>
            <li><strong>Arch</strong> - link to archive</li>
        </ul>
    </li>
    <li>Links given in the <strong>links</strong> parameter are displayed in the order in which they are provided.</li>
</ul>

<h5>Userlinks</h5>
<p>This plugin generates basic links for users.</p>
<p>For logged-in users: Your Account, Profile, Private messages, Logout.</p>
<p>For not logged users: Your Account, Login, Register, Lost password.</p>
<pre>{literal}{ext:Blocks:userlinks:[flat]}{/literal}</pre>
<p>Parameter <strong>flat</strong> is optional and allows you to disable links grouping.</p>
{/gettext}