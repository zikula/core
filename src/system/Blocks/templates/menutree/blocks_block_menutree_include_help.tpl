
<p>{gt text='Block "Tree-like menu (menutree)" allows you to create the extended menu with nested structure. Below you will find some basic information.'}</p>

<h4>{gt text='How to start?'}</h4>
<p>{gt text='To <strong>add the first item</strong> to the menu, click link "<strong>Add</strong>". You will get a form in which you must enter the name of the item and possibly other parameters (title, address, css class). At the top of the form you can find drop-down list with installed languages, which allows to switch between languages.'}</p>
<p>{gt text='While adding a new block, you can <strong>import</strong> an existing menu (supported blocks: "menu", "extmenu", "menutree" and "dynamenu"). To this purpose choose a block from the drop-down list.'}</p>

<h4>{gt text='The basic options'}</h4>
<p>{gt text='To <strong>move</strong> an item, simply <strong>grab a folder icon</strong> and drop it on desired position.'}</p>
<p>{gt text='<strong>Click the name</strong> of an item, to view a <strong>context menu</strong>, which includes the following options:'}</p>
<ul>
    <li>{gt text='<strong>Edit</strong>: opens the edit form'}</li>
    <li>{gt text='<strong>Delete</strong>: removes the selected element and all its subelements'}</li>
    <li>{gt text='<strong>Add new...</strong>'}
        <ul>
            <li>{gt text='<strong>before</strong>: opens a create form and adds the new item before the selected element'}</li>
            <li>{gt text='<strong>after</strong>: opens a create form and adds the new item after the selected element'}</li>
            <li>{gt text='<strong>as child</strong>: opens a create form and adds the new item as a child of the selected element'}</li>
        </ul>
    </li>
    <li>{gt text='<strong>Expand/Collapse this node</strong>: expands or collapses the selected node'}</li>
    <li>{gt text='<strong>Deactivate/Activate</strong>: turns on or off the selected element (and all its subitems) for the current language'}</li>
    <li>{gt text='<strong>State</strong>'}
        <ul>
            <li>{gt text='<strong>Activate for all languages</strong>: turns on the selected element (and all its subitems) for all the languages'}</li>
            <li>{gt text='<strong>Deactivate for all languages</strong>: turns off the selected element (and all its subitems) for all the languages'}</li>
        </ul>
    </li>
</ul>

<h4>{gt text='Multilingual options'}</h4>
<p>{gt text='If the site has many languages installed - the menu will include all language versions. Each menu element can a have separated name and title for each language. The URL address and css class may (but not need) be common for all languages.'}</p>
<p>{gt text='The list of available languages is displayed above the menu (current language is marked). By clicking any language name you can change the displayed language.'}</p>
