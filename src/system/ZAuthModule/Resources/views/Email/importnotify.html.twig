{{ include('@ZikulaZAuthModule/Email/header.txt.twig')|nl2br }}
<p>{{ __f('Welcome to %1$s (%2$s)!', {'%1$s': getModVar('ZConfig', 'sitename'), '%2$s': url('home')}) }}</p>

<p>{{ __f('Hello! This e-mail address (\'%1$s\') has been used to register an account on the \'%2$s\' site.', {'%1$s': user.email, '%2$s': getModVar('ZConfig', 'sitename')}) }}</p>

<p>{{ __('Your account details are as follows:') }}</p>

<p>{{ __f('User name: %sub%', {'%sub%': user.uname}) }}<br />
{{ __f('Password:  %sub%', {'%sub%': user.unHashedPass}) }}</p>
{{ include('@ZikulaZAuthModule/Email/footer.txt.twig')|nl2br }}
