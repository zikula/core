<div class="navbar fixed-top navbar-expand-md navbar-light bg-light">
    <div class="container">
        <div class="navbar-header">
            <a class="navbar-brand" href="{{ pagevars.homepath }}"><i class="fa fa-rocket fa-lg text-success"></i>&nbsp;&nbsp;{{ getModVar('ZConfig', 'sitename') }}</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#themeNavBarCollapse" aria-controls="themeNavBarCollapse" aria-expanded="false" aria-label="{{ __('Toggle navigation') }}">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <div id="themeNavBarCollapse" class="navbar-collapse collapse">
            <ul class="navbar-nav">
                <li>{{ adminPanelMenu() }}</li>
            </ul>
            {% if admin|default %}
                {{ knp_menu_render('zikulaBootstrapThemeAdminMenu', {template: '@ZikulaMenuModule/Override/bootstrap_fontawesome.html.twig'}) }}
            {% else %}
                {{ showblockposition('topnav') }}
                {% if currentUser.loggedIn %}
                    <div class="btn-group navbar-btn ml-auto">
                        <a href="{{ path('zikulausersmodule_account_menu') }}" class="btn btn-info">
                            <i class="fa fa-user"></i>&nbsp;&nbsp;{{ __f('Signed in as %username', {'%username': currentUser.uname}) }}
                        </a>
                        <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <span class="sr-only">{{ __('Toggle Dropdown') }}</span>
                        </button>
                        <div class="dropdown-menu">
                            <a href="{{ path('zikulausersmodule_access_logout') }}" class="dropdown-item">{{ __('Log out') }}</a></li>
                            <a href="{{ path('zikulausersmodule_account_menu') }}" class="dropdown-item">{{ __('My Account') }}</a></li>
                        </div>
                    </div>
                {% endif %}
            {% endif %}
        </div>
    </div>
</div>
