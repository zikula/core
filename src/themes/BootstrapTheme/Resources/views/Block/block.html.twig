<div class="z-block z-blockposition-{{ position|lower }} z-bkey-{{ btype|lower }} z-bid-{{ bid }}">
    {% if title is not empty %}
        <h3>{{ title }}</h3>
    {% endif %}
    {{ content|raw }}
</div>
