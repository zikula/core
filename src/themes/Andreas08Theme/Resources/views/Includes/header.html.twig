<!DOCTYPE html>
<html lang="{{ pagevars.lang }}" xml:lang="{{ pagevars.lang }}" dir="{{ pagevars.langdirection }}">
    <head>
        <meta charset="{{ pagevars.meta.charset }}" />
        <title>{{ pagevars.title }}</title>
        <meta name="description" content="{{ pagevars.meta.description }}" />
        <meta name="keywords" content="{{ pagevars.meta.keywords }}" />
        {{ pageAddVar('stylesheet', asset('bootstrap/css/bootstrap-theme.min.css')) }}
        {{ pageAddVar('stylesheet', zasset('@ZikulaAndreas08Theme:css/fluid960gs/reset.css')) }}
        {{ pageAddVar('stylesheet', zasset('@ZikulaAndreas08Theme:css/fluid960gs/grid.css')) }}{# was $stylepath/fluid960gs/$layout.css #}
        {{ pageAddVar('stylesheet', zasset('@ZikulaAndreas08Theme:css/style.css')) }}
    </head>
    <body>

        <div id="theme_page_container" class="container_16">
            <div id="theme_header">
                <h1 class="title">{# adminpanelmenu #} <a href="{{ app.request.baseUrl }}">{{ getModVar('ZConfig', 'sitename') }}</a></h1>
                <h2 class="slogan">{{ getModVar('ZConfig', 'slogan') }}</h2>
            </div>
