{{ include('@ZikulaAndreas08Theme/Include/header.html.twig') }}

<div id="theme_navigation_bar">
    <ul class="list-inline">
        <li><a href="{{ pagevars.homepath }}">{{ __('Home') }}</a></li>
        {% if hasPermission('ZikulaSettingsModule::','::','ACCESS_ADMIN') %}
        <li><a href="{{ path('zikulasettingsmodule_settings_main') }}">{{ __('Settings') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaExtensionsModule::','::','ACCESS_ADMIN') %}
        <li><a href="{{ path('zikulaextensionsmodule_module_viewmodulelist') }}">{{ __('Extensions') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaBlocksModule::','::','ACCESS_EDIT') %}
        <li><a href="{{ path('zikulablocksmodule_admin_view') }}">{{ __('Blocks') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaUsersModule::','::','ACCESS_MODERATE') %}
        <li><a href="{{ path('zikulausersmodule_useradministration_list') }}">{{ __('Users') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaGroupsModule::','::','ACCESS_EDIT') %}
        <li><a href="{{ path('zikulagroupsmodule_group_adminlist') }}">{{ __('Groups') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaPermissionsModule::','::','ACCESS_ADMIN') %}
        <li><a href="{{ path('zikulapermissionsmodule_admin_index') }}">{{ __('Permission rules') }}</a></li>
        {% endif %}
        {% if hasPermission('ZikulaThemeModule::','::','ACCESS_EDIT') %}
        <li><a href="{{ path('zikulathememodule_theme_view') }}">{{ __('Themes') }}</a></li>
        {% endif %}
    </ul>
</div>

{{ include('@ZikulaAndreas08Theme/Body/' ~ themevars.admin ~ '.html.twig') }}
{{ include('@ZikulaAndreas08Theme/Include/footer.html.twig') }}
