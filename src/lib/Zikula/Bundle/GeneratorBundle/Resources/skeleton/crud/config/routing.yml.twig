{% if 'index' in actions %}
{{ route_name_prefix }}:
    pattern:  /
    defaults: { _controller: "{{ module }}:{{ entity }}:index" }
{% endif %}

{% if 'show' in actions %}
{{ route_name_prefix }}_show:
    pattern:  /{id}/show
    defaults: { _controller: "{{ module }}:{{ entity }}:show" }
{% endif %}

{% if 'new' in actions %}
{{ route_name_prefix }}_new:
    pattern:  /new
    defaults: { _controller: "{{ module }}:{{ entity }}:new" }

{{ route_name_prefix }}_create:
    pattern:  /create
    defaults: { _controller: "{{ module }}:{{ entity }}:create" }
    requirements: { _method: post }
{% endif %}

{% if 'edit' in actions %}
{{ route_name_prefix }}_edit:
    pattern:  /{id}/edit
    defaults: { _controller: "{{ module }}:{{ entity }}:edit" }

{{ route_name_prefix }}_update:
    pattern:  /{id}/update
    defaults: { _controller: "{{ module }}:{{ entity }}:update" }
    requirements: { _method: post|put }
{% endif %}

{% if 'delete' in actions %}
{{ route_name_prefix }}_delete:
    pattern:  /{id}/delete
    defaults: { _controller: "{{ module }}:{{ entity }}:delete" }
    requirements: { _method: post|delete }
{% endif %}
