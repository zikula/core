<?php

namespace {{ namespace }};

class {{ module_name }}Version extends \Zikula_AbstractVersion
{
    public function getMetaData()
    {
        $meta = array();
        $meta['displayname']    = $this->__('{{ vendor }} {{ module_name }}');
        $meta['description']    = $this->__('{{ module_name }} description');
        $meta['url']            = $this->__('{{ (vendor ~ module)|lower }}');
        $meta['version']        = '0.0.1';
        $meta['core_min']       = '1.3.6';
        $meta['securityschema'] = array('{{ module }}::' => '::');

        return $meta;
    }
}